#include <bits/stdc++.h>
using namespace std;

int main(void){
	ios::sync_with_stdio(false); 
	cin.tie(NULL);
	cout.tie(NULL);
	string str;
	cin>>str;
	if(str=="SONGDO"){
		cout<<"HIGHSCHOOL";
	}
	if(str=="CODE"){
		cout<<"MASTER";
	}
	if(str=="2023"){
		cout<<"0611";
	}
	if(str=="ALGORITHM"){
		cout<<"CONTEST";
	}
	return 0;
}
