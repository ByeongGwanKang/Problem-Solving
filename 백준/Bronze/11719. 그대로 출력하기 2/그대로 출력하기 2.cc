#include <iostream>
#include <string>
#include <vector>
#include <math.h>
#include<algorithm>
using namespace std;

int main(void) {
	string str;
	while (!cin.eof()) {
		getline(cin, str);
		cout << str << "\n";
	}

	return 0;
}