#include <bits/stdc++.h> 
using namespace std;
int main(void){
	ios::sync_with_stdio(0); 
	cin.tie(0);
	string a,b;
	getline(cin,a);
	getline(cin,b);
	if(a.length()>=b.length()){
		cout<<"go";
	}
	else cout<<"no";

	return 0;
}
