#include <iostream>
#include <string>
#include <vector>
#include <math.h>
#include <algorithm>
#include<stack>
using namespace std;
int main(void) {
	ios::sync_with_stdio(0); cin.tie(0); //c++입출력만 사용하기
	string str;
	cin >> str;
	cout << ":fan::fan::fan:" << endl;
	cout << ":fan::" << str << "::fan:" << endl;
	cout << ":fan::fan::fan:";

	return 0;
}
