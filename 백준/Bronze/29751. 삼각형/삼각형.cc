#include <bits/stdc++.h>
using namespace std;

int main(void){
	ios::sync_with_stdio(false); 
	cin.tie(NULL);
	cout.tie(NULL);
	int w,h;
	cin>>w>>h;
	cout<<fixed;
	cout.precision(1);
	cout<<(double)w*h/2;
    return 0;
}
