#include <bits/stdc++.h> 
using namespace std;

int main(void){
	ios::sync_with_stdio(false); 
	cin.tie(NULL);
	cout.tie(NULL);
	cout<<fixed;
	cout.precision(6);
	int n;
	cin>>n;
	double PI=3.14159265359;
	cout<<n*n*PI<<"\n"<<n*n*2.0;
	return 0;
}
