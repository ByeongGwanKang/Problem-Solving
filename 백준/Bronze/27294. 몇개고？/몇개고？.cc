#include <bits/stdc++.h> 
using namespace std;

int main(void){
	ios::sync_with_stdio(false); 
	cin.tie(NULL);
	cout.tie(NULL);
	int t,s;
	cin>>t>>s;
	if(t<=11||t>16||s==1){
		cout<<280;
	}
	else cout<<320;
}
